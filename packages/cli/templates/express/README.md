# Zayuno Express provider

Run `npm install && npm run dev`, then `zy test --local --port 3000 --api-key dev-secret`.
Copy `.env.example` to `.env`. The server implements the canonical read-only contract without fake production data.
