{
  "name": "@zayuno/database",
  "version": "1.0.0",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist",
    "generate": "prisma generate",
    "migrate": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy || (prisma migrate resolve --applied 20260816000000_init && prisma migrate deploy)",
    "push": "prisma db push",
    "seed": "tsx src/seed.ts",
    "studio": "prisma studio",
    "lint": "echo 'No lint specified for database'"
  },
  "dependencies": {
    "@prisma/client": "^6.1.0",
    "@zayuno/contracts": "workspace:*",
    "@zayuno/shared": "workspace:*",
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "@types/bcryptjs": "^3.0.0",
    "@types/node": "^22.10.2",
    "prisma": "^6.1.0",
    "rimraf": "^6.0.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.2"
  }
}
